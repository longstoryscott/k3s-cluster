apiVersion: v1
kind: Service
metadata:
  name: selkies-desktop-service
  namespace: steam
  labels:
    app: selkies-desktop
spec:
  selector:
    app: selkies-desktop
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP

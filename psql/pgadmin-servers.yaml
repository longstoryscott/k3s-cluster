apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
  namespace: psql
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "lsm",
          "Group": "Servers",
          "Host": "postgres.psql.svc.cluster.local",
          "Port": 5432,
          "MaintenanceDB": "postgres",
          "Username": "lsm",
          "SSLMode": "prefer",
          "PassFile": "/pgadmin4/.pgpass"
        }
      }
    }

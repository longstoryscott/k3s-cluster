toolkit:
  env:
    - name: CONTAINERD_CONFIG
      value: /var/lib/rancher/k3s/agent/etc/containerd/config.toml
    - name: CONTAINERD_SOCKET
      value: /run/k3s/containerd/containerd.sock
  # - name: CONTAINERD_RUNTIME_CLASS # Optional, defaults to 'nvidia'
  #   value: nvidia
  # - name: CONTAINERD_SET_AS_DEFAULT # Optional, consider your needs
  #   value: "true"
